platform: linux
image_resource:
  type: docker-image
  source:
    repository: amidos/dcind
inputs:
- name: helloworld
run:
  path: sh
  args:
    - -exc
    - |
      source /docker-lib.sh
      start_docker
